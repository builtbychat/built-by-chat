# Public test key: safe for local development only.
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA
# Non-secret Access identifiers. Copy the team URL and application AUD only after
# the Access application is created; placeholders intentionally fail closed.
ACCESS_TEAM_DOMAIN=https://replace-before-access.invalid
ACCESS_AUD=replace-before-access
# Set secrets interactively with `npx wrangler secret put`; never commit values.
# TURNSTILE_SECRET
# IDENTITY_SIGNING_SECRET
# NETWORK_HASH_SECRET
