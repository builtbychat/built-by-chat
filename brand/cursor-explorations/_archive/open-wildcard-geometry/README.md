# Open-wildcard geometry studies

Preserves Codex’s organizing idea (assembled pieces, missing opening, detached contribution) while rejecting pill-burst sparkle silhouettes.

1. `interrupted-asterisk` — typographic stroke with a visible break
2. `modular-void` — non-radial pieces + central void
3. `footnote-bracket` — editorial bracket altered by a tiny input

Do not overwrite `brand/concepts/open-wildcard/`.
