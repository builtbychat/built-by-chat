# Versioned prompts

Templates in `templates/` are public production artifacts. Copy and increment the version before changing behavior. `runs/` contains sanitized run records only; raw chat, full model transcripts, secrets, private moderation data, and personal information do not belong in git.

The operating policy and review loop are in `docs/production/PROMPT-OPERATIONS.md`.
