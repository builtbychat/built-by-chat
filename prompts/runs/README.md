# Sanitized run records

Create a record with `npm run prompt:new -- <episode> <template-slug> <short-title>`. A record documents provenance, scope, hashes, verification, and the human disposition. It must never contain raw chat, full model output, secrets, personal data, private moderation notes, or hidden reasoning.
